#include <iostream>
/*
using std::string;
struct Args
{
	unsigned day;
	unsigned month;
	unsigned year;
};

Args ParseArgs()
{
	std::cout << "Enter date in that format with spaces: <day> <month> <year>" << std::endl;
	string str;
	std::cin >> str;

	Args args;
	std::istringstream iss(str);
	if (iss >> args.day
		|| iss >> args.month 
		|| iss >> args.year)
	{
		throw std::invalid_argument("Not correct argument");
	}

	return args;
}
*/

int main()
{
	std::cout << "Message" << std::endl;
}
